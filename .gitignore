.vscode/

data/scenes/
data/objects/
data/lighting/
data/scenes_outdoor_retry/
data/.blenderkit_token
data/vlm_object_placing/

# v7 policy training data: ignore the placement dirs (large JPEGs), keep the note
data/trajectories/*
!data/trajectories/README.md

blender/

tmp/
repos/
logs/

outputs/
runs/
debug/

trash/

__pycache__/
*.pyc

scripts/_out/

.claude/scheduled_tasks.lock
.claude/settings.local.json

# Large generated HTML galleries
gallery_photo_profiles*.html
photo_profiles.html

# built text anchors (rebuild via scripts/build_text_anchor.py)
assets/*.pt
data/trajectories_full

# downloaded baseline checkpoints (UNIC etc.) — fetch at setup, never commit
weights/

# these two are symlinks on some checkouts; the trailing-slash rules above miss them
blender
data/trajectories
