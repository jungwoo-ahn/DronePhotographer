"""Goal-conditioned video world-action policy modules.

Sub-packages are organized model-family-per-directory so each family
(`cosmos/`, `vla/`, `diffusion_policy/`) owns its model + dataset + trainer.
`common/` holds family-agnostic helpers (goal space, action representation,
annotation loading, reward function).
"""
