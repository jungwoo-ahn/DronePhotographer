# `src/policy/diffusion_policy/`

Placeholder for diffusion-policy baselines (Chi et al. 2023 and follow-ups).

Same layout as `src/policy/cosmos/`. Re-use `src/policy/common/` for the goal-space, action representation, annotation iterators, and reward.
