"""Remaining Qwen VLM modules pending pivot disposition.

Only `prompt.py` remains here, flagged in CLEANUP.md as "discuss before changing"
(potential reuse by an Aesthetic Commentary module). The rest of this package
has moved to `legacy/`; `rotation_utils.py` has moved to `src/utils/`.
"""
