# Qwen2.5-VL
torch
transformers>=5.2.0
qwen-vl-utils
accelerate
deepspeed
tensorboard
tqdm
PyYAML
Pillow
numpy

# Keep these only if you still run scoring/detection scripts
opencv-python
supervision
matplotlib
addict
yapf
pycocotools

# Testing
pytest

# Install GroundingDINO separately (editable, no build isolation):
# python -m pip install --no-build-isolation -e ./repos/GroundingDINO
