# Cosmos Policy (new pipeline)
torch>=2.7.0
diffusers>=0.35.2
transformers>=4.57
accelerate
deepspeed
tensorboard
tqdm
PyYAML
Pillow
numpy

# Preserved: scoring / detection / Blender-side scripts
opencv-python
supervision
matplotlib
addict
yapf
pycocotools

# Legacy Qwen-VL training (kept until legacy/ is deleted)
qwen-vl-utils

# Testing
pytest

# Install separately (editable, no build isolation):
#   python -m pip install --no-build-isolation -e ./repos/GroundingDINO
#   python -m pip install --no-build-isolation flash-attn>=2.7.3
