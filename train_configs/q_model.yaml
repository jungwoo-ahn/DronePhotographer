run:
  run_root: runs
  run_name: q_model
  save_every_steps: 200

data:
  data_root: outputs/DogWalk_260215_092109
  distance_threshold: 1.5
  batch_size: 16
  num_workers: 0
  image_size: 224
  pair_cache_path: null

train:
  max_steps: 2000
  lr: 1e-4
  device: auto

model:
  name: cnn
  action_dim: 6
  num_outputs: 5
  cnn:
    feature_dim: 256
    hidden_dim: 256
