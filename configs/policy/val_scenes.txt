# Frozen scene-level validation manifest (val_split_level: scene).
# Picked as the 4 smallest scenes (73/1945 placements = 3.8% of data) so the
# unseen-scene val split costs almost no training data.
# Both Forest-field assets are held out together: same asset family, visually
# similar — splitting them across train/val would leak.
# DO NOT edit casually: changing this list redefines the validation split.
Forest-field_3c5ba348
Forest-field_665e2d80
Modern-Residential-Building-Facade_7441cb20
Desert-Roadside-Repair-Garage_a1a2b2ac
